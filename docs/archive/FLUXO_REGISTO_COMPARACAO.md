# 🔄 FLUXO DE REGISTO - ANTES vs DEPOIS

**Comparação visual do comportamento do sistema**

---

## ❌ ANTES (COM PROBLEMA)

```
┌─────────────────────────────────────────────────────────┐
│ 1. Utilizador preenche formulário em auth.html         │
└───────────────────┬─────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────┐
│ 2. emailSignupForm.submit()                             │
│    → signUpWithEmail(email, password, name, data)       │
└───────────────────┬─────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────┐
│ 3. Firebase Auth cria utilizador ✅                     │
│    → auth.createUserWithEmailAndPassword()              │
└───────────────────┬─────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────┐
│ 4. Dados guardados em sessionStorage ✅                 │
│    → sessionStorage.setItem('pendingUserData', ...)     │
└───────────────────┬─────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────┐
│ 5. onAuthStateChanged() dispara automaticamente         │
└───────────────────┬─────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────┐
│ 6. Tenta criar perfil no Firestore                      │
│    → createOrUpdateUserProfile(user)                    │
└───────────┬───────────────────────┬─────────────────────┘
            │                       │
     SUCESSO│                       │ERRO
            │                       │
            ▼                       ▼
    ┌───────────────┐       ┌──────────────────┐
    │ Perfil criado │       │ Permission Denied│
    │ no Firestore  │       │ Missing perms ❌ │
    └───────┬───────┘       └────────┬─────────┘
            │                        │
            │                        │
            │    ❌ PROBLEMA AQUI!   │
            │    Ambos redirecionam! │
            │                        │
            ▼                        ▼
┌─────────────────────────────────────────────────────────┐
│ 7. Redirect para dashboard.html (após 500ms)            │
│    → setTimeout(() => window.location = 'dashboard')    │
│                                                          │
│ ❌ Redireciona MESMO com erro no Firestore!            │
└───────────────────┬─────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────┐
│ 8. Resultado:                                            │
│    ✅ Conta criada no Firebase Auth                     │
│    ❌ Perfil NÃO existe no Firestore                    │
│    😕 Utilizador não sabe o que aconteceu              │
│    🔴 App pode crashar ao tentar ler perfil            │
└─────────────────────────────────────────────────────────┘
```

---

## ✅ DEPOIS (CORRIGIDO)

```
┌─────────────────────────────────────────────────────────┐
│ 1. Utilizador preenche formulário em auth.html         │
└───────────────────┬─────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────┐
│ 2. emailSignupForm.submit()                             │
│    → signUpWithEmail(email, password, name, data)       │
└───────────────────┬─────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────┐
│ 3. Firebase Auth cria utilizador ✅                     │
│    → auth.createUserWithEmailAndPassword()              │
└───────────────────┬─────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────┐
│ 4. Dados guardados em sessionStorage ✅                 │
│    → sessionStorage.setItem('pendingUserData', ...)     │
└───────────────────┬─────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────┐
│ 5. ⏱️  Timeout de segurança iniciado (5s)               │
│    Se após 5s ainda estiver em auth.html = erro        │
└───────────────────┬─────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────┐
│ 6. onAuthStateChanged() dispara automaticamente         │
│    ✨ NOVA FLAG: let firestoreSuccess = false          │
└───────────────────┬─────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────┐
│ 7. Tenta criar perfil no Firestore                      │
│    → createOrUpdateUserProfile(user)                    │
└───────────┬───────────────────────┬─────────────────────┘
            │                       │
     SUCESSO│                       │ERRO
            │                       │
            ▼                       ▼
    ┌───────────────┐       ┌──────────────────┐
    │ Perfil criado │       │ Permission Denied│
    │ no Firestore  │       │ Missing perms ❌ │
    │ ✅ SUCCESS!   │       │ ❌ CATCH ERROR   │
    └───────┬───────┘       └────────┬─────────┘
            │                        │
            ▼                        ▼
    ┌───────────────┐       ┌──────────────────┐
    │firestoreSuccess│      │firestoreSuccess  │
    │    = true ✅  │       │   = false ❌     │
    └───────┬───────┘       └────────┬─────────┘
            │                        │
            │                        │
            ▼                        ▼
┌─────────────────────┐   ┌──────────────────────────────┐
│ 8A. SUCESSO PATH    │   │ 8B. ERRO PATH                │
│                     │   │                              │
│ if (firestoreSuccess│   │ if (!firestoreSuccess)       │
│     === true) {     │   │     NÃO redireciona!         │
│                     │   │                              │
│   Aguarda 2s        │   │ Aguarda 5s (timeout)         │
│   ↓                 │   │ ↓                            │
│   Redirect para     │   │ Loading desaparece           │
│   dashboard.html ✅ │   │ ↓                            │
│ }                   │   │ Mostra mensagem:             │
│                     │   │ "⚠️ Conta criada mas erro    │
│                     │   │  ao guardar dados..."        │
│                     │   │ ↓                            │
│                     │   │ Utilizador FICA em auth.html │
└─────────┬───────────┘   └──────────┬───────────────────┘
          │                          │
          ▼                          ▼
┌───────────────────┐     ┌─────────────────────────────┐
│ 9A. Dashboard     │     │ 9B. Auth.html               │
│                   │     │                             │
│ ✅ Tudo OK!       │     │ ❌ Erro visível             │
│ ✅ Perfil existe  │     │ ❌ Perfil não criado        │
│ ✅ App funciona   │     │ 💡 Utilizador informado     │
│                   │     │ 📞 Pode contactar suporte   │
└───────────────────┘     └─────────────────────────────┘
```

---

## 🔑 DIFERENÇAS CHAVE

| Aspecto | ❌ ANTES | ✅ DEPOIS |
|---------|---------|----------|
| **Flag de Controlo** | ❌ Não existe | ✅ `firestoreSuccess` |
| **Redirect com Erro** | ❌ Sim, sempre | ✅ Não, só com sucesso |
| **Timeout de Segurança** | ❌ 500ms fixo | ✅ 2s (sucesso) ou 5s (erro) |
| **Mensagem ao Utilizador** | ❌ Nenhuma | ✅ "Erro ao guardar dados..." |
| **Experiência do Utilizador** | 😕 Confuso | ✅ Informado |
| **Logs de Debug** | ⚠️ Básicos | ✅ Detalhados |

---

## 📊 ESTATÍSTICAS DE CÓDIGO

### `js/auth.js`
- **Linhas modificadas:** ~35 (onAuthStateChanged)
- **Novas variáveis:** 1 (`firestoreSuccess`)
- **Nova lógica:** Redirect condicional
- **Delay:** 500ms → 2000ms

### `js/auth-ui.js`
- **Linhas modificadas:** ~15 (emailSignupForm handler)
- **Novo código:** Timeout de 5s com mensagem
- **Feedback visual:** Mensagem de erro

---

## 🎯 BENEFÍCIOS

### Para o Utilizador
- ✅ Sabe quando algo corre mal
- ✅ Recebe instruções ("contacta suporte")
- ✅ Não fica perdido no dashboard sem dados

### Para o Desenvolvedor
- ✅ Logs detalhados para debug
- ✅ Fácil identificar problemas de permissões
- ✅ Sistema mais robusto e previsível

### Para o Sistema
- ✅ Não cria utilizadores "órfãos" (Auth sem Firestore)
- ✅ Consistência de dados garantida
- ✅ Melhor gestão de erros

---

## 📝 CÓDIGO CRÍTICO

### Trecho Principal (auth.js)
```javascript
// ✨ NOVA FLAG
let firestoreSuccess = false;

try {
  await createOrUpdateUserProfile(user);
  firestoreSuccess = true; // ✅ Só marca sucesso aqui
} catch (error) {
  console.error('❌ Erro:', error);
  firestoreSuccess = false; // ❌ Explícito
}

// ✅ CONDIÇÃO ADICIONAL
if (window.location.pathname.includes('auth.html') && 
    !isRedirecting && 
    firestoreSuccess) { // 🔑 NOVO!
  
  setTimeout(() => {
    window.location.href = 'dashboard.html';
  }, 2000);
}
```

### Trecho de Segurança (auth-ui.js)
```javascript
// ⏱️ TIMEOUT DE SEGURANÇA
setTimeout(() => {
  if (window.location.pathname.includes('auth.html')) {
    hideLoading();
    showMessage('error', '⚠️ Conta criada mas erro ao guardar dados...');
    console.error('🔴 Timeout: Perfil não criado após 5s');
  }
}, 5000);
```

---

**Diagrama completo do sistema corrigido!** 🎉
